# care-taxi-shell-v6

## 目的
骨格だけを確定する版です。

## 固定内容
- 背景は常に白
- 左右にグレー背景を出さない
- 青帯は画面横いっぱい
- 青帯内のハンバーガー、日付、時間、＋予約登録は header-canvas 基準の定位置
- UI本体は中央配置
- デバイス差で変わるのは外側の白い余白だけ
- LOCATION / DETAIL / DAYS / STATUS は ui-canvas 内で固定配置
- 赤いガイド線や補助線は描画しない
- この段階では各エリアの中身は描画しない
